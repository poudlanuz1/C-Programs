//WAP to calculate volume of sphere in C.
#include <stdio.h>
#include <math.h>
#define Pi 3.14
int main(){
	float radius,volume;
	printf("Enter the radius of sphere:");
	scanf("%f",&radius);
	volume=(4.0/3)*Pi*pow(r,2);
	printf("The Volume od Sphere is:%.2f\n",volume);
	return 0;	
}
